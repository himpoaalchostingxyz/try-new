FROM lscr.io/linuxserver/qbittorrent:latest

# Change internal web UI port from default 8080 to 8000
ENV WEBUI_PORT=8000

# Expose the new port (optional but good practice)
EXPOSE 8000

# The base image already has a working CMD/ENTRYPOINT, no need to override
